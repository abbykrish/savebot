// Copy this to config.js (gitignored) and fill in your values
const CONFIG = {
  API_BASE: "http://localhost:3000",
  // After deploying, change to your Vercel URL:
  // API_BASE: "https://savebot-xxx.vercel.app",
};
